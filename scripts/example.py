"""
Example script.
Every script must import "common" at the top level in order to update the base directory,
enabling imports of "game".
"""

import common  # pylint: disable=unused-import

# from game.backend.back import run_backend
